generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Fruta {
  id          String   @id @default(cuid())
  nome        String
  preco       Float
  imagem      String
  descricao   String
}
